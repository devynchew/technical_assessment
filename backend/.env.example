# Database Connection String
# Note: When running via Docker Compose, the host is usually 'db' or 'postgres'
# When running locally (npm run dev), the host is 'localhost'
DATABASE_URL="postgresql://user:password@localhost:5432/csvdb?schema=public"